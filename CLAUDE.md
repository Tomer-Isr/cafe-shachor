# Shachor / שחור — демо-кафе с 3D-героем

Демо-концепт кофейни в Яффо для портфолио визитки `iukhvidov`. Вымышленный бренд.
Задача демо — доказать уровень исполнения, а не продавать кофе.

Контент-источник: `D:\Claude\projects\iukhvidov\plans\cafe-demo\` (легенда, 29 позиций, копирайт).
Приёмы: `D:\Claude\docs\3d-web-playbook.md` + `web-design-playbook.md`.

## Стек
Vite + React 19 + TS · @react-three/fiber + drei + postprocessing · Tailwind v4.
Локали HE (default, RTL) / RU. Деплой — статика на GitHub Pages.

## UI Kit (фиксируется ДО кода, менять только здесь)

```
--ink:     #0a0908   фон, почти чёрный с тёплым уклоном
--surface: #15120f   карточки, поверхности
--stone:   #b9ab97   иерусалимский камень, тёплый нейтральный
--fg:      #ece6dc   текст
--muted:   #93897c   вторичный текст
--accent:  #c8703a   обжаренная медь — только для одного действия на экран
--line:    rgba(236,230,220,.12)
```

- Типографика 4 уровня: display (clamp 3–7rem) → h2 (clamp 1.6–2.6rem) → body 1rem/1.6 → caption .8rem.
  HE: Frank Ruhl Libre + Heebo. RU: Cormorant Garamond + Manrope. Переключается в твиках.
- Spacing — шкала Tailwind (4/8). Радиусы: 0 (прямые углы — камень и бетон, не скругления).
- Цена всегда `<Price>` → `dir="ltr"`, формат `12 ₪`.

## Что НЕ делать
- Не симулировать жидкость — это заявленный потолок проекта.
- Не подключать платные API/ключи. Ассеты только CC0 (Poly Haven HDRI лежит в `public/hdr`).
- Не показывать fake-success брони — бронь заканчивается в WhatsApp.
- Не поднимать перф-планку до PSI 90+: решение Томера — зрелищность важнее (ориентир 70–80).
  Но 60fps на телефоне, пауза сцены, reduced-motion и фолбэк без WebGL — обязательны.
