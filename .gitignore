node_modules
dist
.DS_Store
*.local

# Исходные карты Poly Haven (CC0) — качаются `python render/fetch_tex.py`, в
# репозитории не нужны: каждая по 5-17 МБ. Те, что были добавлены раньше
# (counterseam, wall), git продолжает отслеживать — правило их не выбрасывает.
render/assets/tex/*.jpg

__pycache__/
